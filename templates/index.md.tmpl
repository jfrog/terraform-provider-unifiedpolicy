---
layout: ""
page_title: "JFrog Unified Policy Provider"
description: |-
  The JFrog Unified Policy provider provides resources and data sources to interact with Unified Policy (lifecycle policies, templates, rules) from JFrog Platform.
---

# JFrog Unified Policy Provider

The [JFrog](https://jfrog.com/) Unified Policy provider is used to interact with Unified Policy features from the [JFrog Platform](https://jfrog.com/help/r/jfrog-rest-apis/jfrog-platform-rest-apis). The provider must be configured with the proper credentials before it can be used.

Links to documentation for specific resources and data sources can be found in the table of contents to the left.

This provider requires Artifactory 7.125.0 or later, Xray 3.130.5 or later, and an Enterprise Plus license with AppTrust entitlements.

## Example Usage

```terraform
terraform {
  required_providers {
    unifiedpolicy = {
      source  = "jfrog/unifiedpolicy"
      version = "1.0.0"
    }
  }
}

provider "unifiedpolicy" {
  url          = "https://myinstance.jfrog.io/artifactory"
  access_token = "my-access-token"
}

resource "unifiedpolicy_rule" "example" {
  name        = "Example Rule"
  description = "Example rule for policies"
  template_id = "REPLACE_WITH_TEMPLATE_ID"
  parameters  = []
}
```

## Authentication

The Unified Policy provider supports several ways to authenticate:

### Access Token (Recommended)

The provider requires an Access Token to interact with the Unified Policy API.

```terraform
provider "unifiedpolicy" {
  url          = "https://myinstance.jfrog.io/artifactory"
  access_token = "my-access-token"
}
```

You can also use environment variables:

```bash
export JFROG_URL="https://myinstance.jfrog.io/artifactory"
export JFROG_ACCESS_TOKEN="my-access-token"
```

### API Key (Deprecated)

API keys are deprecated but still supported for backward compatibility.

```terraform
provider "unifiedpolicy" {
  url     = "https://myinstance.jfrog.io/artifactory"
  api_key = "my-api-key"
}
```

## Requirements

- Terraform 1.0 or later (or OpenTofu 1.0+)

{{ .SchemaMarkdown | trimspace }}

## Unified Policy API Endpoints

This provider uses the JFrog Unified Policy API (`/unifiedpolicy/api/v1/policies`) to manage lifecycle policies. Lifecycle policies define the rules and enforcement actions that apply to applications at specific stages of the software development lifecycle (SDLC).

### Lifecycle Policy Management

Lifecycle policies are governance controls that define:
- **Conditions** to check (e.g., CVEs, licenses, evidence requirements)
- **Actions** to take when conditions are met (e.g., `block` to fail promotion, `warning` to allow with violation)
- **Scope** (project-level or application-level)
- **Lifecycle gates** (entry, exit, release) at specific stages

